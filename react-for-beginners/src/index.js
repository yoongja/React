import React from 'react';
import ReactDOM from 'react-dom';
import App from './ToDoList';


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
); //app compnent를 아이디가 root인 엘리먼트에 그린다!
